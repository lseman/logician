{
	"name": "@logician/memory-mcp",
	"version": "0.1.0",
	"private": true,
	"description": "Stdio MCP adapter for @logician/memory",
	"type": "module",
	"bin": {
		"logician-memory-mcp": "src/index.ts"
	},
	"scripts": {
		"start": "bun run src/index.ts",
		"test": "bun test src",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@logician/memory": "0.1.0"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"@types/node": "^22.0.0",
		"bun-types": "^1.3.14",
		"typescript": "^7.0.2"
	}
}
