{
  "name": "@logician/memory",
  "version": "0.1.0",
  "description": "Persistent memory for Logician agent — SQLite-backed with observation capture, synthetic compression, consolidation, and context injection.",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "tsx --test $(find src -name '*.test.ts' -type f)",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.2",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  }
}
