{
	"name": "@logician/memoriam",
	"version": "0.1.0",
	"description": "Standalone memory algorithm — SQLite-backed observation capture, synthetic compression, consolidation, retention scoring, and retrieval with working-memory tiers.",
	"type": "module",
	"main": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"@types/node": "^26.1.2",
		"bun-types": "^1.0.0",
		"typescript": "^7.0.2"
	}
}
